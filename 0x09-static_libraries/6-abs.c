#include "main.h"
/**
 * _abs - computes the absolute value of an interger
 * @n:  intergar to check
 * Return: absolute value of the interger
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
